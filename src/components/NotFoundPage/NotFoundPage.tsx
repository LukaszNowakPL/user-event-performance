import React from 'react';
import {notFoundInfo} from './NotFoundPage.style';

export const NotFoundPage: React.FC = () => {
    return <h4 className={notFoundInfo}>Sorry, page does not exist</h4>;
};
